// @ts-check
const { themes: prismThemes } = require('prism-react-renderer');

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: '212ecom-be Docs',
  tagline: 'Code Intelligence Hub — Role-Based Documentation',
  favicon: undefined,
  url: 'http://localhost',
  baseUrl: '/',
  onBrokenLinks: 'warn',

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  // Treat .md files as CommonMark, not MDX — avoids JSX parse errors
  // from generic type signatures like ResponseEntity<PaymentReceipt> in table cells.
  markdown: {
    format: 'detect',
    hooks: {
      onBrokenMarkdownLinks: 'warn',
    },
  },

  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          path: 'docs',
          routeBasePath: '/',
          sidebarPath: require.resolve('./sidebars.js'),
          // Exclude JSON sidecar files generated by cih-engine
          exclude: ['**/*.json'],
          editUrl: undefined,
        },
        blog: false,
        theme: {
          customCss: require.resolve('./src/css/custom.css'),
        },
      }),
    ],
  ],

  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      navbar: {
        title: '212ecom-be',
        items: [
          {
            type: 'doc',
            docId: 'routes',
            position: 'left',
            label: 'API Routes',
          },
        ],
      },
      footer: {
        style: 'dark',
        copyright: 'Generated by Code Intelligence Hub (yummy-cih)',
      },
      prism: {
        theme: prismThemes.github,
        darkTheme: prismThemes.dracula,
        additionalLanguages: ['java', 'bash', 'json'],
      },
      colorMode: {
        defaultMode: 'light',
        disableSwitch: false,
      },
    }),
};

module.exports = config;
